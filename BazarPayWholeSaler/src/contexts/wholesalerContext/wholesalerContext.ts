import { createContext } from "react";
import { WholesalerContextProps } from "../../@types/contexts/wholesalerContext/wholesalerContextProps.types";

const WholesalerContext = createContext({} as WholesalerContextProps);

export default WholesalerContext;
