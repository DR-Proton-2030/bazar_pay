import { StyleSheet } from "react-native";

export const boxListStyle = StyleSheet.create({
    workList:{
        flexDirection: "row",
        justifyContent: "space-between",
        flexWrap: "wrap",
        gap: 10,
        marginTop: 10,
    }
})